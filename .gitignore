# Credenciais locais (não versionar — remover desta lista se quiser versionar com bypass)
secrets.py

# Chaves embarcadas no build (Tavily etc.) — o repositório é PÚBLICO, uma chave
# commitada aqui vira chave vazada. O modelo versionado é bundled_credentials.example.py
bundled_credentials.py

# Chave privada de assinatura da extensão AutoToca (.crx) — NUNCA versionar
secrets/
*.pem

# Ambiente virtual Python
.venv/
venv/
env/

# Artefatos gerados pelo PyInstaller
build/
dist/
*.spec

# Cache Python
__pycache__/
*.pyc
*.pyo

# Banco de dados local
*.db

# Logs
*.log

# Arquivos de sistema
.DS_Store
Thumbs.db

# Ferramentas de build (baixadas automaticamente pelo BUILD_INSTALLER.bat)
tools/

# Instalador gerado
TocaDoCoelho-*-Setup.exe

# Preview helper local (não versionar)
.claude/launch.json

# WhatsApp Update (WAHA-lite) — gerados localmente, nao versionados
waha-lite/node_modules/
node.exe

# Cache WhatsApp Web.js (sessão do navegador, gerado em runtime)
.wwebjs_cache/
scripts/.wwebjs_cache/

# Logs de build
build_log.txt
