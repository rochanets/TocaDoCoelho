# Dependências da imagem WEB (container), servida por Gunicorn — Fase 0 da
# migração para web multiusuário.
#
# É o conjunto necessário para BOOTAR o app e servir a SPA + o CRUD sobre
# SQLite. As dependências pesadas/opcionais (PDF, OCR, docx, áudio/ML) NÃO
# entram aqui e serão adicionadas conforme cada feature for portada para a
# web; as desktop-only (pywin32, Playwright, Selenium) vão para o "Toca
# Companion". O app importa todas essas de forma guardada (try/except), então
# a ausência apenas desativa a feature específica, sem quebrar o boot.
Flask==2.3.3
Flask-CORS==4.0.0
Werkzeug==2.3.7
requests>=2.31.0
chardet==5.2.0
cryptography>=41.0.0
gunicorn>=21.2.0
# Driver PostgreSQL (Fase 2 — backend web). Só é usado quando DATABASE_URL
# aponta para postgresql://; no modo SQLite (desktop/local) fica ocioso.
psycopg[binary]>=3.1
# Tradução de SQL SQLite→PostgreSQL na borda do wrapper de conexão. Só é
# invocado no backend PostgreSQL.
sqlglot>=25.0

# Fase 7.1 — geração server-side de PDFs core (relatório de relacionamento,
# briefings matinais e revisão semanal). Pillow é dependência explícita porque
# os relatórios podem incorporar logos/fotos locais já autorizados pela ACL.
reportlab>=4.0.0,<5.0.0
Pillow>=10.0.0,<13.0.0

# Fase 7.2 — leitura segura de PDF digital e DOCX. OCR, PDF escaneado e o
# formato legado .doc continuam fora da imagem web.
pdfplumber>=0.11.0,<1.0.0
python-docx>=1.1.0,<2.0.0
